# ניחוש שירים - משחק Web 🎵

משחק ניחוש שירים אינטראקטיבי הבנוי עם Flutter ומוכן לפריסה ב-Netlify.

## ✨ תכונות

- 🎭 **שלב לאה** - 20 שירי קאלט משנות ה-70 וה-80
- 🇮🇱 **שירים ישראליים** - פופ ישראלי, רוק, מזרחי ועוד
- 🌍 **שירים עולמיים** - היטים מכל העולם
- 🎮 **מערכת ניקוד** - צברו נקודות וסמנו התקדמות
- 📱 **רספונסיבי** - עובד במובייל, טאבלט ומחשב
- 🎵 **תצוגות מקדימות** - שימוש ב-iTunes API

## 🚀 פריסה ל-Netlify

### שיטה 1: חיבור ל-Git (מומלץ)

1. העלו את הפרויקט ל-GitHub/GitLab/Bitbucket
2. התחברו ל-[Netlify](https://app.netlify.com)
3. לחצו על "Add new site" → "Import an existing project"
4. בחרו את הריפוזיטורי
5. הגדרות יאותרו אוטומטית מ-`netlify.toml`
6. לחצו "Deploy"

**חשוב**: התקינו את Flutter Plugin ב-Netlify:
- Site settings → Plugins → Install `netlify-plugin-flutter`

### שיטה 2: פריסה ידנית מהירה

```bash
# בנו את הפרויקט
flutter build web --release

# העתיקו את _redirects
cp web/_redirects build/web/_redirects

# העלו ל-Netlify
# גררו את התיקייה build/web ל-Netlify Dashboard
```

### שיטה 3: Netlify CLI

```bash
# התקינו את Netlify CLI
npm install -g netlify-cli

# התחברו
netlify login

# בנו ופרסו
flutter build web --release
cp web/_redirects build/web/_redirects
netlify deploy --prod --dir=build/web
```

## 🛠️ פיתוח מקומי

```bash
# התקנת תלויות
flutter pub get

# הרצה במצב פיתוח
flutter run -d chrome

# בנייה לפרודקשן
flutter build web --release
```

## 📦 קבצים חשובים

- `netlify.toml` - קונפיגורציה של Netlify
- `web/_redirects` - ניתוב SPA
- `build.sh` - סקריפט בנייה אוטומטי
- `NETLIFY_DEPLOYMENT.md` - הוראות מפורטות

## 🎯 דרישות

- Flutter SDK 3.0+
- Dart SDK 3.0+
- חיבור אינטרנט (לתצוגות מקדימות)

## 🔧 התאמות אישיות

### שינוי שירים
ערכו את רשימת השירים ב-`lib/main.dart` בסקשן `levels`.

### עיצוב
כל הצבעים והסגנונות נמצאים ב-`lib/main.dart`.

### תצוגות מקדימות
כרגע משתמשים ב-iTunes API. ניתן לשנות ל-API אחר.

## 📱 תמיכה בפלטפורמות

- ✅ Web (Chrome, Firefox, Safari, Edge)
- ✅ Android
- ✅ iOS
- ✅ Windows
- ✅ macOS
- ✅ Linux

## 🐛 פתרון בעיות

### הסאונד לא מתנגן ב-Web
וודאו שהדפדפן מאפשר autoplay או שהמשתמש לחץ על המסך לפני ניגון.

### 404 בריפרש
וודאו שקובץ `_redirects` קיים ב-`build/web`.

### שגיאות build
```bash
flutter clean
flutter pub get
flutter build web --release
```

## 📄 רישיון

פרויקט זה הוא לשימוש אישי ולימודי.

## 🎵 קרדיטים

- תצוגות מוזיקה מקדימות: iTunes API
- Framework: Flutter
- Hosting: Netlify

---

**נהנים מהמשחק? שתפו עם חברים!** 🎉
